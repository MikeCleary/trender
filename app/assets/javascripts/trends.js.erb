$(document).ready(function(){

  $('#side').on('click', '.trend', trendClick);

});

function trendClick(){
  $('#main').html("<img src='<%= asset_path('loader.gif') %>' class='loader'>");
  var trend_id = $(this).data('id');
  var trend_subject = $(this).data('subject');
  $.ajax({
    url: prefix + '/trends/' + trend_id 
  });
};

